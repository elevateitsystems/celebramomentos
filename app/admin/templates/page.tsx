import { AdminPageHeader, AdminShell } from "@/components/admin";
import { templates } from "@/lib/data";
import { Icon } from "@/components/icons";

export default function AdminTemplatesPage() { return <AdminShell><AdminPageHeader eyebrow="CATALOG / DESIGN SYSTEM" title="Templates" body="The three starting points customers use to make their card their own." action={<button className="rounded-full bg-[#ee5264] px-4 py-3 text-xs font-black text-white">+ New template</button>} /><div className="mt-8 grid gap-5 md:grid-cols-3">{templates.map((template) => <article key={template.id} className="overflow-hidden rounded-2xl border border-[#e4e7ee] bg-white"><div className="relative aspect-[1.25/1] bg-[#f8efe8]"><img src={template.image} alt={template.name} className="h-full w-full object-cover" /><span className="absolute left-4 top-4 rounded-full bg-white px-3 py-1.5 text-[10px] font-black tracking-wider">{template.eyebrow}</span></div><div className="p-5"><h2 className="font-black">{template.name}</h2><p className="mt-2 text-sm leading-6 text-[#737b90]">{template.description}</p><div className="mt-5 flex items-center justify-between"><span className="text-sm font-black">€{template.price.toFixed(2)}</span><button className="flex items-center gap-2 text-xs font-black text-[#ee5264]">Edit <Icon name="arrow" size={14} /></button></div></div></article>)}</div></AdminShell>; }
