"use client";

import { useState } from "react";
import { AdminPageHeader, AdminShell } from "@/components/admin";
import { Toast } from "@/components/site";

export default function AdminSettingsPage() { const [toast, setToast] = useState(""); const [notifications, setNotifications] = useState(true); const [maintenance, setMaintenance] = useState(false); return <AdminShell><AdminPageHeader eyebrow="WORKSPACE" title="Settings" body="Frontend-only settings for the team workspace and future integrations." /><div className="mt-8 grid max-w-[860px] gap-6"><section className="rounded-2xl border border-[#e4e7ee] bg-white p-6 sm:p-8"><h2 className="text-lg font-black">Storefront settings</h2><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-black">Store name<input className="field" defaultValue="Celebra Momentos" /></label><label className="text-sm font-black">Support email<input className="field" defaultValue="hola@celebramomentos.com" /></label><label className="text-sm font-black sm:col-span-2">Announcement bar<input className="field" defaultValue="Envío gratis en pedidos de 35€ o más" /></label></div></section><section className="rounded-2xl border border-[#e4e7ee] bg-white p-6 sm:p-8"><h2 className="text-lg font-black">Preferences</h2><div className="mt-5 divide-y divide-[#eef0f4]"><Toggle label="Admin notifications" body="Receive frontend feedback for catalogue and order actions." checked={notifications} onChange={setNotifications} /><Toggle label="Maintenance mode" body="Prepare a future storefront maintenance state." checked={maintenance} onChange={setMaintenance} /></div><button onClick={() => setToast("Settings saved for this demo session")} className="mt-6 rounded-full bg-[#182443] px-5 py-3 text-sm font-black text-white hover:bg-[#ee5264]">Save settings</button></section><section className="rounded-2xl border border-[#e4e7ee] bg-white p-6 sm:p-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="text-[10px] font-black uppercase tracking-[.16em] text-[#ee5264]">INTEGRATION READY</p><h2 className="mt-1 text-lg font-black">Google Reviews</h2><p className="mt-2 max-w-[560px] text-sm leading-6 text-[#737b90]">The storefront currently shows demo reviews. Connect the client’s Google Business Profile in the backend phase to pull new reviews automatically.</p></div><span className="rounded-full bg-[#fff4d7] px-3 py-1.5 text-[10px] font-black text-[#896816]">Mock source</span></div><button onClick={() => setToast("Google Business Profile connection queued for backend setup")} className="mt-6 rounded-full border border-[#dfd3cc] px-4 py-3 text-xs font-black hover:border-[#ee5264]">Configure Google source</button></section><section className="rounded-2xl border border-[#e4e7ee] bg-[#fff7f1] p-6 sm:p-8"><p className="text-[10px] font-black uppercase tracking-[.16em] text-[#ee5264]">PROJECT HANDOFF</p><h2 className="mt-2 text-lg font-black">Source code ownership</h2><p className="mt-2 text-sm leading-6 text-[#737b90]">The frontend source lives in this project workspace and is ready to be handed over to the client under the agreed project terms.</p></section></div>{toast && <Toast message={toast} onClose={() => setToast("")} />}</AdminShell>; }
function Toggle({ label, body, checked, onChange }: { label: string; body: string; checked: boolean; onChange: (value: boolean) => void }) { return <div className="flex items-center justify-between gap-5 py-4"><div><p className="text-sm font-black">{label}</p><p className="mt-1 max-w-[500px] text-xs leading-5 text-[#737b90]">{body}</p></div><button type="button" onClick={() => onChange(!checked)} aria-pressed={checked} className={`relative h-6 w-11 shrink-0 rounded-full transition ${checked ? "bg-[#ee5264]" : "bg-[#d7dbe3]"}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition ${checked ? "left-6" : "left-1"}`} /></button></div>; }
