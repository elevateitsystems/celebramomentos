"use client";

import Link from "next/link";
import { Icon } from "@/components/icons";
import { AdminPageHeader, AdminShell } from "@/components/admin";
import { useMockDb } from "@/lib/mock-store";
import { StatusBadge } from "@/components/site";

export default function AdminDashboard() { const db = useMockDb(); const stats = [["Total orders", String(db.orders.length), "Persisted locally", "bag"], ["Revenue", `€${db.orders.reduce((total, order) => total + order.amount, 0).toFixed(2)}`, "Demo total", "spark"], ["Customers", String(db.users.length), "Registered locally", "user"], ["Active products", String(db.templates.filter((item) => item.active).length), "Catalog live", "heart"]]; return <AdminShell><AdminPageHeader eyebrow="OVERVIEW" title="Good morning, Sofía" body="Here’s what’s happening with Celebra Momentos today." action={<Link href="/admin/products" className="rounded-full bg-[#182443] px-4 py-3 text-xs font-black text-white hover:bg-[#ee5264]">Manage products</Link>} /><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([label, value, trend, icon]) => <div key={label} className="rounded-2xl border border-[#e4e7ee] bg-white p-5"><div className="flex items-center justify-between"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-[#fff0e8] text-[#ee5264]"><Icon name={icon} size={17} /></span><span className="text-[10px] font-black text-[#24714b]">{trend}</span></div><p className="mt-5 text-sm text-[#737b90]">{label}</p><p className="mt-1 text-2xl font-black">{value}</p></div>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><section className="rounded-2xl border border-[#e4e7ee] bg-white p-5 sm:p-6"><div className="flex items-center justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.16em] text-[#ee5264]">RECENT ACTIVITY</p><h2 className="mt-1 text-lg font-black">Recent orders</h2></div><Link href="/admin/orders" className="text-xs font-black text-[#ee5264]">View all →</Link></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[590px] text-left"><thead><tr className="border-b border-[#e4e7ee] text-[10px] uppercase tracking-[.14em] text-[#9297a4]"><th className="pb-3 font-black">Order</th><th className="pb-3 font-black">Customer</th><th className="pb-3 font-black">Amount</th><th className="pb-3 font-black">Status</th></tr></thead><tbody>{db.orders.slice(0, 5).map((order) => <tr key={order.id} className="border-b border-[#eef0f4] text-sm"><td className="py-4 font-black">{order.id}</td><td className="py-4 text-[#737b90]">{order.shipping.name}</td><td className="py-4 font-black"><span>{order.amount.toFixed(2)} €</span></td><td className="py-4"><StatusBadge tone={order.status === "Entregado" ? "green" : "amber"}>{order.status}</StatusBadge></td></tr>)}</tbody></table>{db.orders.length === 0 && <p className="py-8 text-sm text-[#737b90]">No local orders yet. Complete checkout to populate the dashboard.</p>}</div></section><section className="rounded-2xl border border-[#e4e7ee] bg-[#f8c75e] p-6"><p className="text-[10px] font-black uppercase tracking-[.16em] text-[#806723]">QUICK NOTE</p><h2 className="mt-3 text-2xl font-black leading-tight">Keep the moments moving.</h2><p className="mt-3 text-sm leading-6 text-[#665628]">Orders, customer accounts, templates, prices and messages are using localStorage mock persistence for this frontend phase.</p><Link href="/admin/settings" className="mt-6 inline-flex items-center gap-2 rounded-full bg-[#182443] px-4 py-3 text-xs font-black text-white">Workspace settings <Icon name="arrow" size={15} /></Link></section></div></AdminShell>; }
