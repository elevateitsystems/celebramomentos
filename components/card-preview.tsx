import { templates } from "@/lib/data";

export function CardPreview({ templateId, message, emoji, photoDataUrl, photoZoom = 1, photoPosition = "center", size = "A4", compact = false }: { templateId: string; message: string; emoji: string; photoDataUrl?: string | null; photoZoom?: number; photoPosition?: "center" | "top" | "bottom" | "left" | "right"; size?: "A4" | "A3"; compact?: boolean }) {
  const template = templates.find((item) => item.id === templateId) || templates[0];
  return <div className={`relative overflow-hidden rounded-[12px] bg-[#fff0e8] ${compact ? "aspect-[.9/1]" : size === "A3" ? "aspect-[.78/1]" : "aspect-[.82/1]"}`}><img src={photoDataUrl || template.image} alt={`Vista previa ${template.name}`} className="absolute inset-0 h-full w-full object-cover opacity-60 transition-transform duration-300" style={{ objectPosition: photoPosition, transform: `scale(${photoZoom})` }} /><div className="absolute inset-0 bg-white/50" /><div className={`absolute flex flex-col justify-between rounded-[8px] border border-white/90 bg-white/60 backdrop-blur-[1px] ${compact ? "inset-[7%] p-[7%]" : "inset-[8%] p-[7%]"}`}><div className="flex justify-between"><span className="rounded-full bg-[#182443] px-3 py-1.5 text-[9px] font-black tracking-[.12em] text-white">PARA ALGUIEN ESPECIAL</span><span className="text-xl">{emoji}</span></div><div><p className={`serif max-w-[460px] whitespace-pre-line break-words font-bold leading-[.98] tracking-[-.05em] text-[#182443] ${compact ? "text-2xl" : "text-[clamp(28px,5vw,58px)]"}`}>{message || "Tu mensaje aquí"}</p><div className="mt-5 h-1 w-16 rounded-full bg-[#ee5264]" /></div><div className="flex items-end justify-between"><span className={`serif italic text-[#ee5264] ${compact ? "text-xs" : "text-[clamp(13px,2vw,18px)]"}`}>Con todo mi cariño</span><span className={compact ? "text-lg" : "text-2xl"}>♡</span></div></div></div>;
}
